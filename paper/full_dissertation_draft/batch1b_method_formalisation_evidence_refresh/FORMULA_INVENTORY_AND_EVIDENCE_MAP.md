# Formula Inventory and Evidence Map

| Formula / definition | Target chapter | Purpose | Evidence source | Implementation confidence | Caveat |
| --- | --- | --- | --- | --- | --- |
| 1. Notation set \(\mathcal{D},d,\mathcal{U}_d,\mathcal{I}_d,\mathcal{G}_d,\mathcal{M},m,\hat R,p,V,q,\alpha\) | Chapter 3.1 | Provide consistent framework symbols. | `docs/guides/CANONICAL_DATASET_STANDARD.md`; Batch 1 Chapter 3 | partially verified | This is dissertation notation, not a model architecture. |
| 2. Canonical users, items, and graph \((\mathcal{U}_d,\mathcal{I}_d,\mathcal{G}_d)\) | Chapter 3.1-3.2 | Define the model-independent evaluation space. | `docs/guides/CANONICAL_DATASET_STANDARD.md` | verified from guide/code | Internal model graphs may differ. |
| 3. Model-dataset row \((m,d) \in \mathcal{M}\times\mathcal{D}\) | Chapter 3.2 | Identify the unit of validation and reporting. | `reports/tables/canonical_native_path_status_matrix.csv` | partially verified | Amazon rows have partial coverage. |
| 4. Canonical mappings \(\phi_U^{m,d}\) and \(\phi_I^{m,d}\) | Chapter 3.2 | Return internal identifiers to canonical user and item spaces. | `docs/guides/CANONICAL_DATASET_STANDARD.md`; model-view mapping requirements | partially verified | The functions formalise the documented mapping contract rather than one shared implementation. |
| 5. Top-\(K\) list \(\hat R_u^{K,m,d}=[i_{u,1},\ldots,i_{u,K}]\) | Chapter 3.3 | Define reportable recommendation order. | `scripts/validation/evaluate_uid_topk.py`; canonical dataset standard | verified from guide/code | Native-path lists may be short; missing slots remain non-hits. |
| 6. Native path \(p_{u,i}^{m,d}=[(r_0,t_0,e_0),\ldots,(r_L,t_L,e_L)]\) | Chapter 3.3 | Define the explanation evidence structure. | `docs/guides/PATH_METRICS_GUIDE.md`; `scripts/validation/validate_xrecsys_export.py` | verified from guide/code | Exact relation vocabularies are dataset-specific. |
| 7. \(\mathrm{ValidPath}(p)=\mathbf 1[e_0=u\land e_L=i\land i\in\hat R_u]\) | Chapter 3.3 | Formalise endpoint and recommendation alignment. | `scripts/validation/validate_xrecsys_export.py` | partially verified | The code performs these checks separately rather than as one named predicate. |
| 8. Export package \(\mathcal E_{m,d}=(T_{m,d},P_{m,d},X_{m,d})\) | Chapter 3.3 | Define the minimum native-path evidence contract. | Canonical dataset standard; path-metrics guide; export validator | verified from guide/code | Accuracy-only references have a different scope. |
| 9. Validation gate \(V_{m,d}=\prod G_{\cdot}\) | Chapter 3.4 | Convert registered checks into reportability eligibility. | `scripts/validation/validate_xrecsys_export.py`; validation summaries | partially verified | The product is dissertation formalisation of distinct code checks. |
| 10. Strict HR@K | Chapter 3.4 | Measure whether a user has at least one relevant recommendation. | `scripts/validation/evaluate_uid_topk.py` | verified from guide/code | Strict metric only; not a sweep value. |
| 11. Strict Precision@K | Chapter 3.4 | Measure relevant top-k slots using denominator \(K\). | Canonical dataset standard; strict evaluator | verified from guide/code | Short-list missing slots count as non-hits. |
| 12. Strict Recall@K | Chapter 3.4 | Measure recovered relevant items. | `scripts/validation/evaluate_uid_topk.py` | verified from guide/code | Requires canonical test labels. |
| 13. Strict DCG@K and NDCG@K | Chapter 3.4 | Measure rank-discounted strict relevance. | `scripts/validation/evaluate_uid_topk.py`; canonical dataset standard | verified from guide/code | IDCG uses \(\min(K,\lvert R_u^+\rvert)\); separate from sweep NDCG. |
| 14. Path anchors \(a(p)=e_1\), \(b(p)=e_{L-1}\), \(\tau(p)=r_L\) | Chapter 3.4 | Identify the seed item, bridge entity, and path type. | `docs/guides/PATH_METRICS_GUIDE.md`; `xrecsys/metrics.py` | verified from guide/code | Applies to the registered repository path format. |
| 15. \(\mathrm{LIR}(p)=\rho_u(a(p))\) | Chapter 3.4 | Formalise linked-interaction recency. | Path-metrics guide; `xrecsys/metrics.py`; `xrecsys/path_data_loader.py` | verified from guide/code | Timestamp weighting and missing-anchor handling are repository-specific. |
| 16. \(\mathrm{SEP}(p)=\sigma_d(b(p))\) | Chapter 3.4 | Formalise the bridge-entity degree-derived SEP weight. | Path-metrics guide; `xrecsys/metrics.py`; `xrecsys/path_data_loader.py` | requires manual verification | The anchor lookup is verified, but the guide states lower-degree / higher-weight semantics while the matrix-generation code appears to assign increasing EMA weight after ascending degree sorting. |
| 17. \(\mathrm{ETD}(u)=\lvert\{\tau(p_{u,i})\}\rvert/\lvert\mathcal T_d\rvert\) | Chapter 3.4 | Formalise user-level path-type diversity. | Path-metrics guide; `xrecsys/metrics.py`; `xrecsys/myutils.py` | verified from guide/code | The legal type denominator is dataset-specific. |
| 18. User- and dataset-level aggregation \(Q_{u,m,d}^q\), \(Q_{m,d}^q\) | Chapter 3.4 | Aggregate LIR and SEP over validated explanations and users. | `xrecsys/metrics.py` | partially verified | The displayed average applies to aligned, metric-eligible paths; exact missing-anchor handling and SEP weight direction follow code and require reconciliation. |
| 19. \(\hat R_{u,\alpha}^{K,m,d,q}=\operatorname{TopK}(S_\alpha^{m,d,q})\) | Chapter 3.6 | Represent objective-specific alpha-sweep output. | `xrecsys/main.py`; `xrecsys/optimizations.py` | abstract notation only | \(S_\alpha\) is implementation-specific. |
| 20. Universal linear alpha score combination | Chapter 3.6 | Consider whether one linear rule covers LIR, SEP, and ETD. | `xrecsys/optimizations.py` | not added | LIR and SEP use linear candidate weighting, but ETD uses bins and an unseen-type bonus; one universal formula would be inaccurate. |
| 21. Explanation delta \(\Delta q_{m,d}=Q_{m,d}^q(1)-Q_{m,d}^q(0)\) | Chapter 4.1 | Summarise registered endpoint movement. | Batch 1 Chapter 4; canonical sweep CSVs | abstract notation only | Descriptive difference, not a new result or inferential statistic. |
| 22. Sweep NDCG delta \(\Delta\mathrm{NDCG}_{m,d}^{\mathrm{sweep},q}\) | Chapter 4.1 | Pair ranking response with explanation movement. | Canonical sweep CSVs | abstract notation only | Must not replace strict NDCG@10. |
| 23. Ablation baseline preservation \(D_{\mathrm{base}}\) | Chapter 5.1 | Formalise exact alpha-zero list preservation. | `reports/tables/ablation/pgpr_ucpr_path_module/alpha0_baseline_preservation.csv`; ablation script | verified from guide/code | Limited to the registered PGPR/UCPR frozen-item-set protocol. |
| 24. \(\mathrm{Retention}_{m,d,q}(\alpha)=\mathrm{NDCG}^{\mathrm{abl}}(\alpha)/\mathrm{NDCG}^{\mathrm{abl}}(0)\) | Chapter 5.1 | Formalise ablation NDCG retention. | `scripts/analysis/run_pgpr_ucpr_path_module_ablation.py`; ablation tables | verified from guide/code | Ablation NDCG is not strict six-model accuracy. |
| 25. \(\alpha^*=\arg\max_\alpha Q_{m,d}^q(\alpha)\) subject to retention \(\ge0.95\) | Chapter 5.1 | Formalise the registered operating-point rule. | Ablation script; `main_ablation_table_95pct_ndcg.csv` | verified from guide/code | Does not define a universal operating point or model superiority. |
| 26. Eligibility and boundary sets \(\mathcal A=\{(m,d):V=1\}\), \(\mathcal B=\{(m,d):V=0\}\) | Chapter 5.4 | Separate performance evidence from boundary evidence. | Canonical status matrix; export validation summaries | partially verified | PASS records contract conformity; blocked rows are not performance measurements. |
